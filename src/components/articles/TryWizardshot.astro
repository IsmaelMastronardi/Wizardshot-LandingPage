---
import { Image } from 'astro:assets';
import whiteArrow from '../../../public/images/whiteArrow.png';
---

<article class="">
  <p class="font-alphabetizedCassetteTapes font-bold text-lg text-end">100% free, forever &#x1F911;</p>
  <div class="bg-black text-white text-start rounded-t-lg">
    <div class="p-4">
      <h3 class="text-2xl font-medium mb-3">Create Tutorials in 3 Clicks.</h3>
      <p class="text-sm text-customLightGray opacity-60">With WizardShot, You Click Capture, And We’ll Capture What’s Happening on Your Screen And Turn It Into A Tutorial.</p>
      <div class="flex justify-end py-4">
        <div class="w-44 bgGradient">
          <p class="text-xl underline bgGradient w-full flex items-center justify-between">Try Wizardshot
            <Image src={whiteArrow} alt="whiteArrow" />
          </p>
        </div>
      </div>
    </div>
  </div>
</article>

<style>
  article {
    z-index: 2;
  }
  .bgGradient {
	position: relative;
}
.bgGradient:before {
    content: "";
    z-index: 10;
    position: absolute;
    top: 0;
    right: 0;
    bottom: 0;
    left: 0;
    background: linear-gradient(90deg, rgba(68, 255, 154, 0.6) 0%,rgba(68, 176, 255, 0.3) 20%, rgba(139, 68, 255, 0.4) 60%, rgba(255, 102, 68, 0.3) 80%, rgba(235, 255, 112, 0.6) 100%);
    transform: translate3d(0px, 2px, 0) scale(0.95);
    filter: blur(25px);
    opacity: var(0.7);
    transition: opacity 0.3s;
    border-radius: inherit;
}

.bgGradient::after {
    content: "";
    z-index: -1;
    position: absolute;
    top: 0;
    right: 0;
    bottom: 0;
    left: 0;
    background: inherit;
    border-radius: inherit;
}
</style>